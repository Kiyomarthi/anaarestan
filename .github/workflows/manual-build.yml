name: Build Nuxt

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 20
          cache: "pnpm"

      - name: Setup pnpm
        uses: pnpm/action-setup@v2
        with:
          version: 8 # یا هر نسخه‌ای که پروژه می‌خواد

      - name: Install dependencies
        run: pnpm install

      - name: Copy .env
        run: cp .env .env

      - name: Build Nuxt
        run: pnpm run build
